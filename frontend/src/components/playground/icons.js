// Import icons from your preferred icon library (e.g., Material UI, Heroicons)
import { Settings, Mic, Camera, Share, Chat, ChevronRight, Check } from '@mui/icons-material';

export const icons = {
    Settings,
    Mic,
    Camera,
    Share,
    Chat
};

// Add the missing icons
export const ChevronIcon = ChevronRight;
export const CheckIcon = Check;

export default icons; 